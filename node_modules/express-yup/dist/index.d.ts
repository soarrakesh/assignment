import { NextFunction, Request, Response } from 'express';
import { Schema, ValidateOptions } from 'yup';
export declare function validate<S extends any, C = object>(schema: Schema<S, C>, validateOptions?: ValidateOptions<C>): (req: Request, res: Response, next: NextFunction) => Promise<void>;
